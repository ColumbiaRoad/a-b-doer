import {
	append,
	clear,
	insertBefore,
	insertAfter,
	prepend,
	pollQuerySelector,
	pollQuerySelectorAll,
	waitFor,
	waitElement,
	waitElements,
	setDefaultTimeout,
} from 'a-b-doer';

window.utilFns = {
	append,
	clear,
	insertBefore,
	insertAfter,
	prepend,
	pollQuerySelector,
	pollQuerySelectorAll,
	waitFor,
	waitElement,
	waitElements,
	setDefaultTimeout,
};
