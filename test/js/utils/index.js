import {
	append,
	clear,
	insertBefore,
	prepend,
	pollQuerySelector,
	pollQuerySelectorAll,
	waitFor,
	waitElement,
	waitElements,
} from 'a-b-doer';

window.utilFns = {
	append,
	clear,
	insertBefore,
	prepend,
	pollQuerySelector,
	pollQuerySelectorAll,
	waitFor,
	waitElement,
	waitElements,
};
